<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="description" content="<%= description %>">
  <title>
    <%= title %> | Portfolio Artist
  </title>
  <link rel="shorcut icon" href="../../src/assets/icons/painter.png">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css">
  <link rel="stylesheet" href="../assets/styles.css">
  <style>
    body {
      background-color: #000;
      color: #fff;
      font-family: 'Inter', sans-serif;
      display: flex;
      align-items: center;
      justify-content: center;
      height: 100vh;
      margin: 0;
    }

    .login-card {
      background: #111;
      border: 1px solid #333;
      border-radius: 12px;
      padding: 2.5rem;
      width: 100%;
      max-width: 400px;
      box-shadow: 0 10px 30px rgba(0, 0, 0, 0.5);
    }

    .login-card h2 {
      font-weight: 700;
      margin-bottom: 1.5rem;
      text-align: center;
      color: #ffc107;
      text-transform: uppercase;
      letter-spacing: 2px;
    }

    .form-control {
      background-color: #222;
      border: 1px solid #444;
      color: #fff;
      padding: 0.8rem;
      margin-bottom: 1rem;
    }

    .form-control:focus {
      background-color: #2a2a2a;
      border-color: #ffc107;
      color: #fff;
      box-shadow: none;
    }

    .btn-login {
      background-color: #ffc107;
      border: none;
      color: #000;
      font-weight: 700;
      padding: 0.8rem;
      text-transform: uppercase;
      letter-spacing: 1px;
      transition: all 0.3s ease;
    }

    .btn-login:hover {
      background-color: #e0a800;
      transform: translateY(-2px);
    }

    .back-home {
      display: block;
      text-align: center;
      margin-top: 1.5rem;
      color: #aaa;
      text-decoration: none;
      font-size: 0.9rem;
    }

    .back-home:hover {
      color: #fff;
    }
  </style>
</head>

<body>
  <div class="login-card">
    <h2>
      <%= title %>
    </h2>

    <% if (typeof error !=='undefined' && error) { %>
      <div class="alert alert-danger py-2 small">
        <%= error %>
      </div>
      <% } %>

        <form action="/auth/register" method="post">
          <div class="mb-3">
            <label class="form-label text-secondary small">Email</label>
            <input type="email" name="email" class="form-control" placeholder="votre@email.com" required>
          </div>
          <div class="mb-3">
            <label class="form-label text-secondary small">Mot de passe</label>
            <input type="password" name="password" class="form-control" placeholder="••••••••" required>
          </div>
          <button type="submit" class="btn btn-login w-100 mt-3">Créer mon compte</button>
        </form>
        <p class="text-center mt-3 small text-secondary">
          Déjà inscrit ? <a href="/auth/login" class="text-warning">Connectez-vous</a>
        </p>
        <a href="/" class="back-home">← Retour au portfolio</a>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>